# Voyage en terre juridique

Livre Kirby 5. Contenu dans `content/home/`.

Custom : plugin `book-kit`, snippets `book/*`, modèles chapitre / sous-chapitre / accueil.
Notes : structure `wordnotes`, bloc `gloss`, tag `(annot: … note: …)`.
