name: field.blocks.list.name
icon: list-bullet
wysiwyg: true
preview: list
fields:
  text:
    label: field.blocks.list.name
    type: list
    marks:
      - bold
      - italic
      - underline
      - strike
      - code
      - sup
      - sub
      - link
      - email
